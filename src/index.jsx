import React from 'react';
import { render } from 'ink';
import ViewManager from './ViewManager.jsx';

// Render the Ink-based CLI application with `ViewManager` as the root component
render(<ViewManager />);
